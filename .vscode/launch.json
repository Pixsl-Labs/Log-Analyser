{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Log Analyser",
            "type": "python",
            "request": "launch",
            "module": "app.main",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}